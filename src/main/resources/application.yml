server:
  port: 8090
  servlet:
    context-path: /api
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_hr?useSSL=true
    username: root
    password: '!@#mhl123123'
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

application:
  cors:
    allowed-origins: http://localhost:4200,http://localhost:4201
  web:
    upload-path: E:\resources\static

